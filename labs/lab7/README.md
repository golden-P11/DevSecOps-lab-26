# Lab 7 — Container Security

Reference files in this directory enable the **lab7-Container Security** CI workflow to pass on the course repo.

For **Lab 7.2**, you must write your own hardened manifests — do not copy these verbatim into your submission. For **Lab 7.3**, write your own Conftest policy using Rego v1 syntax (`deny contains msg if { ... }`).

Do not commit `results/` — scan output is regeneratable.
